const settings = {
    endpoint: "https://rickandmortyapi.com"
}

const url = {
    character: settings.endpoint + "/api/character",
    episode: settings.endpoint + "/api/episode"
}

export { settings, url };